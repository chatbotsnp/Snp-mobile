name: android_release (disabled)

on:
  workflow_dispatch:  # chỉ chạy khi bấm tay

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use build-android.yml instead."
