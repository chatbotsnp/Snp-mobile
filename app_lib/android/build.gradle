// Top-level Gradle file for the Android host project.
// Bổ sung repositories cho toàn bộ subprojects (AGP 8.x + Flutter 3.19.x)

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
